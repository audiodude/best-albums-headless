# <%= raw album.artist %>- <%= raw album.title %>
=> https://open.spotify.com/album/<%= raw album.spotify_id %> On Spotify
-- Added <%= raw album.created_at.strftime("%Y-%m-%d") %> --

<%= raw album.to_gemini %>
